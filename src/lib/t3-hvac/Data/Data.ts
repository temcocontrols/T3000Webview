
// import { ref } from 'vue'
// Placeholder: Vue ref - will need React equivalent
const ref = (val: any) => ({ value: val });

export const insertT3EntryDialog = ref({ active: false, data: null })

export default {}
