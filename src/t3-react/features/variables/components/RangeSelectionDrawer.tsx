/**
 * Range Selection Drawer for Variables
 * Variables use the same range system as inputs, so we re-export the inputs drawer
 */

export { RangeSelectionDrawer } from '../../inputs/components/RangeSelectionDrawer';
